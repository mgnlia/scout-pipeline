# Scout Pipeline

Structured opportunity intelligence for the AI Office. Each scan cycle produces a machine-readable JSON pipeline file and a human-readable summary.

## Structure

```
scans/
  cycle-10/
    pipeline.json    # Machine-readable opportunity data
    summary.md       # Human-readable findings
```

## Schema

See `schema/opportunity.json` for the opportunity data format.
